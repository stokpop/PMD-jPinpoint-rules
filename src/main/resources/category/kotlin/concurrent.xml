<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="jpinpoint-kotlin-concurrent-rules" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd" xmlns="http://pmd.sourceforge.net/ruleset/2.0.0">
    <description>jPinpoint rule set for performance aware Kotlin coding related to concurrency, sponsored by Rabobank.</description>

    <rule name="AvoidCompletionServiceTake"
          language="kotlin"
          message="Avoid completionService.take, use poll"
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md">
        <description>
            Problem: take() stalls indefinitely in case of hanging threads and consumes a thread.&#13;
            Solution: use poll() with a timeout value and handle the timeout.
            (jpinpoint-rules)
        </description>
        <priority>2</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading,performance,pitfall" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
//PostfixUnaryExpression[
    pmd-kotlin:matchesSig('java.util.concurrent.CompletionService#take()')
    and ./PostfixUnarySuffix/NavigationSuffix[@Identifier='take']
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
fun collectBad(completionService: CompletionService<List<Any>>): Future<List<Any>> {
    return completionService.take() // bad
}

fun collectGood(completionService: CompletionService<List<Any>>): Future<List<Any>>? {
    return completionService.poll(3L, TimeUnit.SECONDS) // good
}
        ]]></example>
    </rule>

    <rule name="AvoidFutureGetWithoutTimeout"
          language="kotlin"
          message="Avoid future.get without timeout"
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#ia06">
        <description>
            Problem: Stalls indefinitely in case of hanging threads and consumes a thread.&#13;
            Solution: Provide a timeout value and handle the timeout.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading,performance" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
//PostfixUnaryExpression[
    pmd-kotlin:matchesSig('java.util.concurrent.Future#get()')
    and ./PostfixUnarySuffix/NavigationSuffix[@Identifier='get']
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
fun bad(complFuture: CompletableFuture<String>): String {
    return complFuture.get() // bad
}

fun good(complFuture: CompletableFuture<String>): String {
    return complFuture.get(10, TimeUnit.SECONDS) // good
}
        ]]></example>
    </rule>

    <rule name="AvoidFutureJoinWithoutTimeout"
          language="kotlin"
          message="Avoid future.join without timeout"
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#ia07">
        <description>
            Problem: Stalls indefinitely in case of hanging thread(s) and consumes a thread.&#13;
            Solution: Provide a timeout before the join and handle the timeout. For example a future.get(timeout, unit), an orTimeout() or a completeOnTimeout(). You may want to use CompletableFuture.allOf() too.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading,performance,pitfall" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
(: future.join() without any timeout method anywhere in the same function body :)
//PostfixUnaryExpression[
    pmd-kotlin:matchesSig('java.util.concurrent.Future#join()')
    and ./PostfixUnarySuffix/NavigationSuffix[@Identifier='join']
    and not(
        ancestor::FunctionBody//PostfixUnaryExpression[
            pmd-kotlin:matchesSig('java.util.concurrent.Future#get(_,java.util.concurrent.TimeUnit)')
            or pmd-kotlin:matchesSig('java.util.concurrent.CompletableFuture#orTimeout(_,java.util.concurrent.TimeUnit)')
            or pmd-kotlin:matchesSig('java.util.concurrent.CompletableFuture#completeOnTimeout(_,_,java.util.concurrent.TimeUnit)')
        ]
    )
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
fun getOrdersBad(futures: List<CompletableFuture<Order>>): List<Order> {
    return futures
        .filter { it.isDone && !it.isCompletedExceptionally }
        .map { it.join() } // bad, no timeout provided
}

fun getOrdersGood(futures: List<CompletableFuture<Order>>): List<Order> {
    val allResult = CompletableFuture.allOf(*futures.toTypedArray())
    try {
        allResult.get(5L, TimeUnit.SECONDS) // good - timeout
    } catch (e: Exception) { }
    return futures
        .filter { it.isDone && !it.isCompletedExceptionally }
        .map { it.join() } // good - timeout provided above
}
        ]]></example>
    </rule>

    <rule name="AvoidUnguardedAssignmentToNonFinalFieldsInSharedObjects"
          language="kotlin"
          message="Avoid unguarded assignments to non-final fields in objects shared among threads. Access thread-safely AND use @GuardedBy."
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#tutc07">
        <description>
            Problem: Multiple threads typically access fields of a singleton or may access fields in session scoped objects. Unguarded assignment to a non-final (var) field is thread-unsafe and may cause corruption or visibility problems. It may also unintentionally mix-up session data.&#13;
            Solution: Make the properties val and unmodifiable. If they really need to be mutable, make access thread-safe: use @Synchronized and @GuardedBy or @Volatile.&#13;
            Notes&#13;
            1. For autowiring/injection, the assignment of the reference is thread safe, so var is acceptable. The unmodifiable/thread-safe requirement for that field still holds. Also make sure no other thread-unsafe assignment is made to that field.&#13;
            2. In case you are sure the Component is used in single threaded context only (e.g. a Tasklet), annotate the class with @NotThreadSafe to make this explicit.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="data-mix-up,jpinpoint-rule,multi-threading" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
(: Kotlin: AvoidUnguardedAssignmentToNonFinalFieldsInSharedObjects :)
(: Find assignments to var fields inside singleton/shared-object classes :)

(: Step 1: Match classes annotated as singletons or shared objects :)
//ClassDeclaration[
  (
       pmd-kotlin:hasAnnotation('Service')
    or pmd-kotlin:hasAnnotation('Component')
    or pmd-kotlin:hasAnnotation('Controller')
    or pmd-kotlin:hasAnnotation('Repository')
    or pmd-kotlin:hasAnnotation('RestController')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Service')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Component')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Controller')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Repository')
    or pmd-kotlin:hasAnnotation('org.springframework.web.bind.annotation.RestController')
  )
  (: exclude request/prototype scope — not shared :)
  and not(pmd-kotlin:hasAnnotation('RequestScope')
       or pmd-kotlin:hasAnnotation('org.springframework.web.context.annotation.RequestScope'))
  (: exclude @Scope("request") or @Scope("prototype") :)
  and not(.//Annotation//T-Identifier[@Text='Scope']
      and .//Annotation//T-LineStrText[@Text='request' or @Text='prototype'])
  (: exclude @NotThreadSafe :)
  and not(pmd-kotlin:hasAnnotation('NotThreadSafe')
       or pmd-kotlin:hasAnnotation('net.jcip.annotations.NotThreadSafe'))
  (: exclude @ConfigurationProperties :)
  and not(pmd-kotlin:hasAnnotation('ConfigurationProperties')
       or pmd-kotlin:hasAnnotation('org.springframework.boot.context.properties.ConfigurationProperties'))
]
(: Step 2: Find assignments where the LHS is a var field name :)
//Assignment[
  (: LHS identifier matches a var field of the enclosing class body :)
  (DirectlyAssignableExpression|AssignableExpression)//T-Identifier[
    (
      (: Case A: direct assignment like "mutableField = value" or "this.mutableField = value" :)
      @Text = ancestor::ClassBody[1]//PropertyDeclaration[
        T-VAR
        and not(ancestor::FunctionBody)
        (: not @Volatile :)
        and not(pmd-kotlin:hasAnnotation('Volatile') or pmd-kotlin:hasAnnotation('kotlin.jvm.Volatile'))
        (: not @GuardedBy :)
        and not(pmd-kotlin:hasAnnotation('GuardedBy') or pmd-kotlin:hasAnnotation('net.jcip.annotations.GuardedBy')
             or pmd-kotlin:hasAnnotation('javax.annotation.concurrent.GuardedBy'))
      ]/VariableDeclaration/@Identifier

      (: ensure this is not just a local var or param with the same name :)
      and not(
        (: not a function parameter :)
        not(ancestor::DirectlyAssignableExpression//T-Identifier[@Text='this'])
        and @Text = ancestor::FunctionDeclaration//FunctionValueParameter/Parameter/@Identifier
      )
      and not(
        (: not a local var (PropertyDeclaration inside FunctionBody) :)
        not(ancestor::DirectlyAssignableExpression//T-Identifier[@Text='this'])
        and @Text = ancestor::FunctionBody//PropertyDeclaration/VariableDeclaration/@Identifier
      )
    )
    or
    (
      (: Case B: inherited field — class extends a base :)
      ancestor::ClassDeclaration//DelegationSpecifiers
      (: not in an init block (constructor-like) :)
      and not(ancestor::ClassBody/AnonymousInitializer)
      (: the name is NOT a known var field of THIS class :)
      and not(@Text = ancestor::ClassBody[1]//PropertyDeclaration[not(ancestor::FunctionBody)]/VariableDeclaration/@Identifier)
      (: and not a local var :)
      and not(@Text = ancestor::FunctionBody//PropertyDeclaration/VariableDeclaration/@Identifier)
      (: and not a function parameter :)
      and not(@Text = ancestor::FunctionDeclaration//FunctionValueParameter/Parameter/@Identifier)
    )
  ]
  (: not inside a @Synchronized function :)
  and not(ancestor::FunctionDeclaration[pmd-kotlin:hasAnnotation('Synchronized') or pmd-kotlin:hasAnnotation('kotlin.jvm.Synchronized')])
  (: not inside a synchronized {} block — Kotlin uses synchronized(lock) {} :)
  and not(ancestor::Statement[
    preceding-sibling::*[1][self::Statement]//T-Identifier[@Text='synchronized']
  ] (: simplified check :) )
  (: not inside an @Autowired / @Inject / @PostConstruct / @BeforeStep / @Value annotated function :)
  and not(ancestor::FunctionDeclaration[
       pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
    or pmd-kotlin:hasAnnotation('PostConstruct') or pmd-kotlin:hasAnnotation('javax.annotation.PostConstruct') or pmd-kotlin:hasAnnotation('jakarta.annotation.PostConstruct')
    or pmd-kotlin:hasAnnotation('BeforeStep') or pmd-kotlin:hasAnnotation('org.springframework.batch.core.annotation.BeforeStep')
    or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
    or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
  ])
  (: not in afterPropertiesSet function :)
  and not(ancestor::FunctionDeclaration[SimpleIdentifier/T-Identifier[@Text='afterPropertiesSet']])
  (: not in a nested object declaration (static-like inner class) :)
  and not(ancestor::ObjectDeclaration[ancestor::ClassBody])
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
@Service
class MyService {
    @Value("\${batchService.http.url}")
    var url: String = ""

    @GuardedBy("lock")
    var guardedUrl: String = ""

    fun setUrlBad(url: String) {
        this.url = url // bad: unguarded accessor method
    }

    @Synchronized
    fun setUrlGood(url: String) {
        this.url = url // good: synchronized accessor method
    }
}
        ]]></example>
    </rule>

    <rule name="AvoidNonPrivateFieldsInSharedObjects"
          language="kotlin"
          message="Avoid non-private properties in shared objects. Make properties private to ensure thread-safety."
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#tutc11">
        <description>
            Problem: A singleton, or more general: an object shared among threads, has a property that is not private.
            These properties can possibly be modified from other classes.&#13;
            Solution: Make the properties private.
            Note: properties are excluded when annotated with: @Autowired/@Value/@Inject.
            (jpinpoint-rules)</description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
(: Kotlin: AvoidNonPrivateFieldsInSharedObjects :)
(: Find non-private properties in singleton/shared-object classes :)

(: Step 1: Match classes annotated as singletons or shared objects :)
//ClassDeclaration[
  (
       pmd-kotlin:hasAnnotation('Service')
    or pmd-kotlin:hasAnnotation('Component')
    or pmd-kotlin:hasAnnotation('Controller')
    or pmd-kotlin:hasAnnotation('Repository')
    or pmd-kotlin:hasAnnotation('RestController')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Service')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Component')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Controller')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Repository')
    or pmd-kotlin:hasAnnotation('org.springframework.web.bind.annotation.RestController')
  )
  (: exclude request/prototype scope — not shared :)
  and not(pmd-kotlin:hasAnnotation('RequestScope')
       or pmd-kotlin:hasAnnotation('org.springframework.web.context.annotation.RequestScope'))
  (: exclude @Scope("request") or @Scope("prototype") :)
  and not(.//Annotation//T-Identifier[@Text='Scope']
      and .//Annotation//T-LineStrText[@Text='request' or @Text='prototype'])
  (: exclude @NotThreadSafe :)
  and not(pmd-kotlin:hasAnnotation('NotThreadSafe')
       or pmd-kotlin:hasAnnotation('net.jcip.annotations.NotThreadSafe'))
]
(: Step 2: Find non-private, non-val properties at class level :)
//PropertyDeclaration[
  (: not inside a function body — class-level property :)
  not(ancestor::FunctionBody)
  (: not private :)
  and not(pmd-kotlin:modifiers()='private')
  (: not val (final) :)
  and not(T-VAL)
  (: not injection framework annotated :)
  and not(
       pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
    or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
    or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
  )
  (: not in a nested class inside the class body :)
  and not(ancestor::ClassDeclaration[ancestor::ClassBody])
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
@Controller
class RootController {

    var baskets: List<Any> = listOf()          // bad, not private
    protected var baskets2: List<Any> = listOf() // bad, not private
    val baskets3: List<Any> = listOf()          // good, val (final)
    private var baskets4: List<Any> = listOf()  // good, private

    @Autowired
    lateinit var baskets5: List<Any>           // allowed, injected
}
        ]]></example>
    </rule>

    <rule name="AvoidCommonPoolForFutureAsync"
          language="kotlin"
          message="Avoid using the common thread pool for future.supplyAsync, use a separate pool."
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#ia08">
        <description>
            Problem: CompletableFuture.supplyAsync is typically used for remote calls. By default, it uses the common pool.
            The number of threads in the common pool is equal to the number of CPUs, which is suitable for in-memory processing.
            For I/O, however, this number is typically not suitable because most time is spent waiting for the response and not in CPU.
            The common pool must not be used for blocking calls.&#13;
            Solution: A separate, properly sized, pool of threads (an Executor) should be used for the async calls.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading,performance,pitfall,unpredictable" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
//PostfixUnaryExpression[
    pmd-kotlin:matchesSig('java.util.concurrent.CompletableFuture#supplyAsync(_)')
    and ./PostfixUnarySuffix/NavigationSuffix[@Identifier='supplyAsync']
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
class Foo {
    private val asyncPool = Executors.newFixedThreadPool(8)

    fun bad(accounts: List<Any>) {
        val futures = accounts.map {
            CompletableFuture.supplyAsync { isAccountBlocked(it) } // bad, uses common pool
        }
    }

    fun good(accounts: List<Any>) {
        val futures = accounts.map {
            CompletableFuture.supplyAsync({ isAccountBlocked(it) }, asyncPool) // good
        }
    }
}
        ]]></example>
    </rule>

    <rule name="AvoidUnguardedAssignmentToNonFinalFieldsInObjectsUsingSynchronized"
          language="kotlin"
          message="Avoid unguarded assignments to non-final fields in objects using synchronized. Access thread-safely AND use @GuardedBy."
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#tutc07">
        <description>
            Problem: Multiple threads typically access fields of an object using synchronized. Unguarded assignment to a non-final (var) field is thread-unsafe and may cause corruption or visibility problems. To make this thread-safe, that is, guard the field e.g. with @Synchronized methods, may cause contention.&#13;
            Solution: Make the properties val and unmodifiable. If they really need to be mutable, make access thread-safe: use @Synchronized and @GuardedBy or @Volatile.&#13;
            Notes:&#13;
            1. In case you are sure the class is used in a single threaded context only, remove the current use of synchronized and annotate the class with @NotThreadSafe to make this explicit.&#13;
            2. Use @VisibleForTesting for methods (e.g. setters) used for JUnit only.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
(: Kotlin: AvoidUnguardedAssignmentToNonFinalFieldsInObjectsUsingSynchronized :)
(: Find assignments to var fields in classes that use synchronized :)

(: Step 1: Match classes that use synchronized and are not @NotThreadSafe :)
//ClassDeclaration[
  not(pmd-kotlin:hasAnnotation('NotThreadSafe')
       or pmd-kotlin:hasAnnotation('net.jcip.annotations.NotThreadSafe'))
  and (
    (: has @Synchronized annotated function :)
    .//FunctionDeclaration[pmd-kotlin:hasAnnotation('Synchronized') or pmd-kotlin:hasAnnotation('kotlin.jvm.Synchronized')]
    (: or uses synchronized() block :)
    or .//T-Identifier[@Text='synchronized']
  )
]
(: Step 2: Find unguarded assignments to var fields :)
//Assignment[
  (DirectlyAssignableExpression|AssignableExpression)//T-Identifier[
    @Text = ancestor::ClassBody[1]//PropertyDeclaration[
      T-VAR
      and not(ancestor::FunctionBody)
      (: not @Volatile :)
      and not(pmd-kotlin:hasAnnotation('Volatile') or pmd-kotlin:hasAnnotation('kotlin.jvm.Volatile'))
      (: not @GuardedBy :)
      and not(pmd-kotlin:hasAnnotation('GuardedBy') or pmd-kotlin:hasAnnotation('net.jcip.annotations.GuardedBy')
           or pmd-kotlin:hasAnnotation('javax.annotation.concurrent.GuardedBy'))
    ]/VariableDeclaration/@Identifier
    (: not a function parameter :)
    and not(
      not(ancestor::DirectlyAssignableExpression//T-Identifier[@Text='this'])
      and @Text = ancestor::FunctionDeclaration//FunctionValueParameter/Parameter/@Identifier
    )
    (: not a local var :)
    and not(
      not(ancestor::DirectlyAssignableExpression//T-Identifier[@Text='this'])
      and @Text = ancestor::FunctionBody//PropertyDeclaration/VariableDeclaration/@Identifier
    )
  ]
  (: not inside a @Synchronized function :)
  and not(ancestor::FunctionDeclaration[pmd-kotlin:hasAnnotation('Synchronized') or pmd-kotlin:hasAnnotation('kotlin.jvm.Synchronized')])
  (: not inside a synchronized {} block :)
  and not(ancestor::Statement[
    preceding-sibling::*[1][self::Statement]//T-Identifier[@Text='synchronized']
  ])
  (: not inside framework-annotated functions :)
  and not(ancestor::FunctionDeclaration[
       pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
    or pmd-kotlin:hasAnnotation('PostConstruct') or pmd-kotlin:hasAnnotation('javax.annotation.PostConstruct') or pmd-kotlin:hasAnnotation('jakarta.annotation.PostConstruct')
    or pmd-kotlin:hasAnnotation('BeforeStep') or pmd-kotlin:hasAnnotation('org.springframework.batch.core.annotation.BeforeStep')
    or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
    or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
  ])
  (: not in afterPropertiesSet function :)
  and not(ancestor::FunctionDeclaration[SimpleIdentifier/T-Identifier[@Text='afterPropertiesSet']])
  (: not in a nested object declaration :)
  and not(ancestor::ObjectDeclaration[ancestor::ClassBody])
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
class Bad {
    var date: String = ""

    fun setDate(d: String) {
        date = d // bad - synchronized used in class
    }

    @Synchronized
    fun getDate(): String = date
}

@NotThreadSafe
class Good {
    var date: String = ""

    fun setDate(d: String) {
        date = d // good - @NotThreadSafe
    }
}
        ]]></example>
    </rule>

    <rule name="AvoidUnguardedMutableFieldsInSharedObjects"
          language="kotlin"
          message="Avoid unguarded non-final or mutable fields in objects shared among threads. Make final immutable or access thread-safely AND use @GuardedBy."
          class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
          externalInfoUrl="${doc_root}/JavaCodePerformance.md#tutc07">
        <description>
            Problem: Multiple threads typically access fields of a singleton or may access fields in session scoped objects. If a field or its reference is mutable, access is thread-unsafe and may cause corruption or visibility problems. It may also unintentionally mix-up session data.&#13;
            Solution: Make the properties val and unmodifiable. If they really need to be mutable, make access thread-safe: use @Synchronized and @GuardedBy or @Volatile.&#13;
            Notes:&#13;
            1. Instances of Date, StringBuilder, URL and File are examples of mutable objects and should be avoided (or else guarded) as fields of shared objects.&#13;
            2. Instances of classes like ArrayList, HashMap and HashSet are also mutable and should be properly wrapped with e.g. Collections.unmodifiableList after initialization, or use thread-safe implementations like ConcurrentHashMap.&#13;
            3. For autowiring/injection, the assignment of the reference is thread safe, so var is acceptable. The unmodifiable/thread-safe requirement for that field still holds.&#13;
            4. In case you are sure the Component is used in single threaded context only, annotate the class with @NotThreadSafe to make this explicit.
            (jpinpoint-rules)
        </description>
        <priority>1</priority>
        <properties>
            <property name="tags" value="jpinpoint-rule,multi-threading,data-mix-up" type="String" description="classification"/>
            <property name="xpath">
                <value><![CDATA[
(: Kotlin: AvoidUnguardedMutableFieldsInSharedObjects :)
(: Find non-final/non-volatile var fields or mutable-typed fields in singleton classes :)

(: Step 1: Match singleton/shared-object classes :)
//ClassDeclaration[
  (
       pmd-kotlin:hasAnnotation('Service')
    or pmd-kotlin:hasAnnotation('Component')
    or pmd-kotlin:hasAnnotation('Controller')
    or pmd-kotlin:hasAnnotation('Repository')
    or pmd-kotlin:hasAnnotation('RestController')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Service')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Component')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Controller')
    or pmd-kotlin:hasAnnotation('org.springframework.stereotype.Repository')
    or pmd-kotlin:hasAnnotation('org.springframework.web.bind.annotation.RestController')
  )
  (: exclude request/prototype scope :)
  and not(pmd-kotlin:hasAnnotation('RequestScope')
       or pmd-kotlin:hasAnnotation('org.springframework.web.context.annotation.RequestScope'))
  and not(.//Annotation//T-Identifier[@Text='Scope']
      and .//Annotation//T-LineStrText[@Text='request' or @Text='prototype'])
  (: exclude @NotThreadSafe :)
  and not(pmd-kotlin:hasAnnotation('NotThreadSafe')
       or pmd-kotlin:hasAnnotation('net.jcip.annotations.NotThreadSafe'))
  (: exclude @ConfigurationProperties unless has Setter annotation :)
  and not(
    (pmd-kotlin:hasAnnotation('ConfigurationProperties')
     or pmd-kotlin:hasAnnotation('org.springframework.boot.context.properties.ConfigurationProperties'))
    and not(pmd-kotlin:hasAnnotation('Setter') or pmd-kotlin:hasAnnotation('lombok.Setter'))
  )
]
(: Step 2: Find var fields that are not guarded :)
//PropertyDeclaration[
  not(ancestor::FunctionBody)
  and not(ancestor::ClassDeclaration[ancestor::ClassBody])
  and (
    (: Case 1: var field that is not volatile and not guarded :)
    T-VAR
    and not(pmd-kotlin:modifiers()='volatile')
    and not(pmd-kotlin:hasAnnotation('Volatile') or pmd-kotlin:hasAnnotation('kotlin.jvm.Volatile'))
    (: not annotated with framework/GuardedBy :)
    and not(
         pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
      or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
      or pmd-kotlin:hasAnnotation('GuardedBy') or pmd-kotlin:hasAnnotation('net.jcip.annotations.GuardedBy')
           or pmd-kotlin:hasAnnotation('javax.annotation.concurrent.GuardedBy')
    )
    (: @Value field excluded if no unguarded assignment elsewhere in the class :)
    and not(
      (pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value'))
      and not(
        VariableDeclaration/@Identifier = ancestor::ClassDeclaration[1]//Assignment//T-Identifier[
          not(ancestor::FunctionDeclaration[
               pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
            or pmd-kotlin:hasAnnotation('PostConstruct') or pmd-kotlin:hasAnnotation('javax.annotation.PostConstruct') or pmd-kotlin:hasAnnotation('jakarta.annotation.PostConstruct')
            or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
            or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
          ])
        ]/@Text
      )
    )
    (: not assigned only in framework-annotated functions :)
    and not(
      VariableDeclaration/@Identifier = ancestor::ClassDeclaration[1]//FunctionDeclaration[
           pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
        or pmd-kotlin:hasAnnotation('PostConstruct') or pmd-kotlin:hasAnnotation('javax.annotation.PostConstruct') or pmd-kotlin:hasAnnotation('jakarta.annotation.PostConstruct')
        or pmd-kotlin:hasAnnotation('BeforeStep') or pmd-kotlin:hasAnnotation('org.springframework.batch.core.annotation.BeforeStep')
        or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
        or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
      ]//Assignment//(DirectlyAssignableExpression|AssignableExpression)//T-Identifier/@Text
      and not(
        VariableDeclaration/@Identifier = ancestor::ClassDeclaration[1]//FunctionDeclaration[not(
             pmd-kotlin:hasAnnotation('Autowired') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Autowired')
          or pmd-kotlin:hasAnnotation('PostConstruct') or pmd-kotlin:hasAnnotation('javax.annotation.PostConstruct') or pmd-kotlin:hasAnnotation('jakarta.annotation.PostConstruct')
          or pmd-kotlin:hasAnnotation('BeforeStep') or pmd-kotlin:hasAnnotation('org.springframework.batch.core.annotation.BeforeStep')
          or pmd-kotlin:hasAnnotation('Value') or pmd-kotlin:hasAnnotation('org.springframework.beans.factory.annotation.Value')
          or pmd-kotlin:hasAnnotation('Inject') or pmd-kotlin:hasAnnotation('javax.inject.Inject') or pmd-kotlin:hasAnnotation('jakarta.inject.Inject')
        )]//Assignment//(DirectlyAssignableExpression|AssignableExpression)//T-Identifier/@Text
      )
    )
  )
  or
  (
    (: Case 2: field of known mutable type, even if val :)
    not(pmd-kotlin:hasAnnotation('GuardedBy') or pmd-kotlin:hasAnnotation('net.jcip.annotations.GuardedBy')
         or pmd-kotlin:hasAnnotation('javax.annotation.concurrent.GuardedBy'))
    and (
      pmd-kotlin:typeIs('java.util.Date')
      or pmd-kotlin:typeIs('java.lang.StringBuilder')
      or pmd-kotlin:typeIs('java.lang.StringBuffer')
      or pmd-kotlin:typeIs('java.net.URL')
      or pmd-kotlin:typeIs('java.io.File')
      or pmd-kotlin:typeIs('java.util.ArrayList')
      or pmd-kotlin:typeIs('java.util.HashMap')
      or pmd-kotlin:typeIs('java.util.HashSet')
    )
  )
]
                ]]></value>
            </property>
        </properties>
        <example><![CDATA[
@Component
class Bad {
    var url: String = ""  // bad, unguarded var field

    fun setUrlBad(url: String) {
        this.url = url
    }
}

@Component
class BadMutableTypes {
    val date: Date = Date()                           // bad, Date is mutable
    @Volatile var strBuilder: StringBuilder = StringBuilder() // bad, mutable type
    val map: HashMap<String, String> = HashMap()      // bad, mutable collection

    @GuardedBy("this")
    val guardedMap: HashMap<String, String> = HashMap() // good, guarded
}
        ]]></example>
    </rule>

</ruleset>
